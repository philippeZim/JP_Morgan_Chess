package Model
/*
class Stack[T] {
    var stack : List[T]
    
    def push(element : T): Unit = {
        stack = element :: stack
    }
    
    def pop(): Option[T] = {
        stack match {
            case Nil => None;
            case h::t => 
                stack = t
                Some(h)
        }
    }
}
 */
